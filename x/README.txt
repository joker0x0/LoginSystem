You need only to set Server Name, Username, Password, DB Name and CharSet in the dbh.php file on x/includes/dbh.php
Set values to the variable from line "11" to "15"

The database must have the following

a table called users,
in users table must include: id, firstname, lastname, username, passwordt

to be able to work probably

Hope this was useful :) .
